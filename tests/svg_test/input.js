import { Store } from "cx/data";
import {
  Url,
  History,
  Widget,
  startHotAppLoop,
  enableCultureSensitiveFormatting,
} from "cx/ui";
import { Timing, Debug } from "cx/util";

var x = (param) => (
  <cx>
    <A test />
  </cx>
);
